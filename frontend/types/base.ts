export interface Base {
  id: number;
  userId: number;
  name: string;
  birthPlace: string;
  birthDate: string;
  motherName: string;
  socialSecNumber: string;
  taxId: string;
  email: string;
}
